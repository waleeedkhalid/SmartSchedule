"use client";
import type React from "react";

/**
 * Persona: Shared
 * Goal: Display a bell icon with unread count and trigger notifications panel.
 * Note: Atomic, reusable across personas. Placeholder only.
 */
export function NotificationsBell(): React.ReactNode {
  return null;
}

export default NotificationsBell;
